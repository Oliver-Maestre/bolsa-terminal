<network-security-config>
    <!-- Cleartext HTTP is only ever allowed to the emulator's host alias and
         localhost, for local backend development. The cloud backend uses
         HTTPS and needs no exception. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
